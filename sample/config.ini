# SPA-CGAL Boolean Operation Configuration
# Keys are case-insensitive. Lines beginning with # or ; are comments.
#
# Supported operations : UNION  INTERSECTION  DIFFERENCE
# Supported mesh formats: .off  .obj  .ply  .stl
#
# Paths may be absolute or relative to the working directory from which
# BooleanOp.exe is launched.

primary_model   = sample/cube_a.off
secondary_model = sample/cube_b.off
operation       = UNION
output_path     = output/result.off
